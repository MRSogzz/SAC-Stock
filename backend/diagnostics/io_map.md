# IO Map

_Auto-generated by `diagnostics/Registry.py`. Do not edit manually._

_Last updated: 2026-05-18 18:24:26_

| Module | Function | Input | Output | Notes | Calls |
|--------|----------|-------|--------|-------|------:|
| Proc | `compute_features` | `df: pd.DataFrame` | `pd.DataFrame` | Build 35 Tier 1 features and apply 252-day rolling z-score. | 0 |
| Proc | `align_features` | `stocks: dict[str, pd.DataFrame]` | `dict[str, pd.DataFrame]`, `dict[str, np.ndarray]`, `dict[str, np.ndarray]`, `list[str]`, `list[str]` | Align stocks, apply cross-sectional z-score, append benchmark market state. | 0 |
| Proc | `scale_features` | `feat_dict: dict[str, pd.DataFrame]`, `scaler_dict: dict | None` | `dict[str, np.ndarray]`, `dict` | Deprecated compatibility shim. Features are already rolling-z normalized. | 0 |
| Model | `PortfolioActorLogitDelta.__init__` | `state_dim: int`, `n_stocks: int`, `hidden: int` | `PortfolioActorLogitDelta` | Variant H：SharedFeatureExtractor + Actor MLP，10*32+19 → N_ACTIONS | 0 |
| Model | `PortfolioActorDirichlet.__init__` | `state_dim: int`, `n_stocks: int`, `hidden: int` | `PortfolioActorDirichlet` | 舊版 Dirichlet+Beta Actor（Run A/C） | 0 |
| Model | `PortfolioCritic.__init__` | `state_dim: int`, `n_stocks: int`, `hidden: int` | `PortfolioCritic` | Variant H Twin Q-network（Shared Extractor + Xavier uniform init） | 0 |
| Model | `PortfolioCritic.forward` | `state: torch.Tensor (B, STATE_DIM)`, `action: torch.Tensor (B, N_STOCKS)` | `torch.Tensor (B, 1)`, `torch.Tensor (B, 1)` | Variant H Twin Q forward（Shared Extractor） | 0 |
| Model | `PortfolioCritic.q_min` | `state: torch.Tensor (B, STATE_DIM)`, `action: torch.Tensor (B, N_STOCKS)` | `torch.Tensor (B, 1)` | 回傳 min(Q1, Q2) | 0 |
| Utils | `calc_fee` | `amount: float`, `is_sell: bool`, `is_odd: bool` | `float` | 整張買：ceil(×0.1425%)≥20元；零股買：≥1元；賣出額外加 ceil(×0.3%) 證交稅 | 0 |
| Utils | `calc_shares` | `amount: float`, `price: float` | `int`, `int`, `float`, `float` | 整張優先，不足一張改零股；回傳 (整張數, 零股數, 整張花費, 零股花費) | 0 |
| Utils | `calc_mdd` | `portfolio_curve: list[float]` | `float` | 最大回撤（MDD）= min((arr - peak) / peak)，全為 0 時回傳 0.0 | 0 |
| Utils | `calc_sharpe` | `returns: list[float]`, `risk_free_daily: float` | `float` | 年化夏普比率 = (mean(ex) / std(ex)) × √250；少於 2 筆或 std≈0 回傳 0.0 | 0 |
| Utils | `calc_sortino` | `returns: list[float]`, `risk_free_daily: float` | `float` | 年化 Sortino = (mean(ex) / std(下行)) × √250；無下行波動回傳 inf | 0 |
| Utils | `calc_win_rate` | `trade_log: list[dict]` | `float` | 從交易記錄計算勝率（%）；只計算含 profit 的賣出記錄 | 0 |
| Utils | `annualize_return` | `total_return: float`, `n_days: int` | `float` | 累積報酬率（%）→ 年化報酬率（%），假設一年 250 個交易日 | 0 |
| Utils | `now_str` |  | `str` | 回傳當前時間字串，格式 YYYY-MM-DD HH:MM:SS | 0 |
| Utils | `date_str` | `dt: datetime | pd.Timestamp` | `str` | pandas Timestamp 或 datetime 轉字串，格式 YYYY-MM-DD | 0 |
| Utils | `sanitize` | `obj: dict | list | np.ndarray | scalar` | `dict | list | scalar` | 遞迴清理 dict/list，nan/inf → 0.0；numpy 型別轉 Python 原生型別 | 0 |
| Utils | `safe_float` | `v: Any`, `default: float` | `float` | 安全轉 float；None / NaN / inf / 無法轉換 → 回傳 default | 0 |
| Utils | `safe_list` | `lst: list`, `default: float` | `list[float]` | 對列表中每個元素套用 safe_float，清理 nan/inf | 0 |
| Engine | `run_backtest` | `actor: PortfolioActor`, `feat_dict: dict[str, pd.DataFrame]`, `prices_dict: dict[str, np.ndarray]`, `volumes_dict: dict[str, np.ndarray]`, `stock_ids: list[str]`, `initial_capital: float`, `feat_names: list[str]`, `dates: list[str]` | `dict` | Deterministic policy 全程回測；含整張/零股/升級/強制平倉；回傳 portfolio_curve / trade_log / metrics | 0 |
| Env | `CompositeRewardV10.__init__` |  | `CompositeRewardV10` | 舊版 Composite Reward（Run A/B） | 0 |
| Env | `LinearDownsideReward.__init__` |  | `LinearDownsideReward` | 線性獎勵（Run C/D），warmup 500 步後鎖定縮放常數 c | 0 |
| Env | `PortfolioEnv.__init__` | `features_dict: dict[str, pd.DataFrame]`, `prices_dict: dict[str, np.ndarray]`, `volumes_dict: dict[str, np.ndarray]`, `scalers: dict | None`, `initial_capital: float`, `reward_mode: str` | `PortfolioEnv` | v7：支援兩種 reward（composite/linear），輸出 cost_t，現金計入 MAR | 0 |
| Env | `PortfolioEnv.reset` |  | `np.ndarray (STATE_DIM,)` | 重置環境狀態，回傳初始觀測 | 0 |
| Env | `PortfolioEnv.step` | `action: np.ndarray (N_TRADEABLE,)` | `np.ndarray (STATE_DIM,)`, `float`, `bool` | v7：無死區，輸出 cost_t，現金計入 MAR | 0 |
| Env | `PortfolioEnv.portfolio_value` |  | `float` | 回傳當前總資產 / 初始資金（從實際持倉即時計算） | 0 |
| Agent | `ReplayBuffer.__init__` | `capacity: int` | `ReplayBuffer` | 環形緩衝區，預設容量 SAC_BUFFER_SIZE；超過 capacity 時自動覆蓋最舊資料 | 0 |
| Agent | `ReplayBuffer.push` | `s: np.ndarray (STATE_DIM,)`, `a: np.ndarray (N_STOCKS,)`, `r: float`, `s_: np.ndarray (STATE_DIM,)`, `done: bool` |  | 推入一筆 transition；超過 capacity 自動丟棄最舊的 | 0 |
| Agent | `ReplayBuffer.sample` | `batch: int` | `torch.Tensor (B, STATE_DIM)`, `torch.Tensor (B, N_STOCKS)`, `torch.Tensor (B, 1)`, `torch.Tensor (B, STATE_DIM)`, `torch.Tensor (B, 1)` | 隨機不重複抽樣 batch 筆，回傳 FloatTensor 並搬到 DEVICE | 0 |
| Env | `AsymmetricLinearDownsideReward.__init__` |  | `AsymmetricLinearDownsideReward` | P2：LinearDownside + 非對稱 pnl 放大，放大在 raw 層 | 0 |
| Agent | `SACAgentDirichlet.__init__` | `state_dim: int`, `n_stocks: int` | `SACAgentDirichlet` | 舊版 Dirichlet Agent（Run A/C），grad_clip=0.3 | 0 |
| Agent | `SACAgentDirichlet.act` | `obs: np.ndarray (STATE_DIM,)`, `deterministic: bool` | `np.ndarray (N_STOCKS,)` | stochastic/deterministic action，回傳前 N_STOCKS 維 | 0 |
| Agent | `SACAgentDirichlet.update` |  | `float`, `float`, `float` | SAC 三段更新；grad_clip=0.3 | 0 |
| Agent | `SACAgentLogitDelta.__init__` | `state_dim: int`, `n_stocks: int` | `SACAgentLogitDelta` | LogitDelta Agent（Run B/D），管理 logit_state，grad_clip=0.3 | 0 |
| Agent | `SACAgentLogitDelta.act` | `obs: np.ndarray (STATE_DIM,)`, `deterministic: bool` | `np.ndarray (N_STOCKS,)` | LogitDelta act()：注入 logit_state，更新後儲存新狀態 | 0 |
| Agent | `SACAgentLogitDelta.update` |  | `float`, `float`, `float` | LogitDelta SAC 三段更新；Phase 1 梯度解耦；IQN Critic + Regime 條件化 | 0 |
| Engine | `train` | `period: str`, `episodes: int`, `initial_capital: float`, `val_days: int`, `on_episode: Callable | None` | `dict` | SAC 訓練主流程：載入資料 → 切分訓練/驗證 → 接續訓練 → 回測 → 儲存模型 | 0 |
| Engine | `validate` | `period: str`, `val_days: int`, `initial_capital: float`, `on_episode: Callable | None` | `dict` | 載入模型 → 取最後 val_days 筆資料 → run_backtest → 回傳驗證集指標 | 0 |
| Engine | `predict_next` | `period: str` | `dict` | 載入模型 → 取最新一天特徵 → deterministic 推論 → 回傳建議倉位與動作 | 0 |
| Engine | `train_window` | `run_id: str`, `window: int`, `feat_dfs: dict`, `prices_dict: dict`, `volumes_dict: dict`, `dates: list`, `train_start_idx: int`, `train_end_idx: int`, `val_start_idx: int`, `val_end_idx: int`, `initial_capital: float`, `episodes: int`, `on_episode: Callable | None` | `dict` | v7：四路實驗矩陣的單窗口訓練，含 Logit 幾何監控 | 0 |
| Engine | `train_experiment_matrix` | `period: str`, `initial_capital: float`, `episodes: int`, `runs: list[str]`, `on_episode: Callable | None` | `dict` | v7：四路實驗矩陣（B/D），順序跑，各自儲存獨立模型 | 0 |
| Engine | `train_walkforward` | `period: str`, `initial_capital: float`, `episodes: int`, `run_id: str`, `on_episode: Callable | None` | `dict` | 單一 Run 的 Walk-forward（向後相容），預設 Run D | 0 |
| Engine | `predict_walkforward` | `period: str`, `run_id: str` | `dict` | detect_regime → 選對應窗口模型 → 最新特徵推論 → 輸出各股建議倉位 | 0 |
